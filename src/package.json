{
  "name": "sisenco",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "seed": "node seed/seedDatabase.js",
    "seed:reset": "node seed/resetDatabase.js",
    "test": "cross-env NODE_ENV=test EMAIL_ENABLED=false NODE_OPTIONS=--experimental-vm-modules jest --setupFilesAfterEnv=./tests/setup.js --runInBand",
    "test:watch": "cross-env NODE_ENV=test EMAIL_ENABLED=false NODE_OPTIONS=--experimental-vm-modules jest --setupFilesAfterEnv=./tests/setup.js --watch",
    "test:coverage": "cross-env NODE_ENV=test EMAIL_ENABLED=false NODE_OPTIONS=--experimental-vm-modules jest --setupFilesAfterEnv=./tests/setup.js --coverage --runInBand",
    "test:unit": "cross-env NODE_ENV=test EMAIL_ENABLED=false NODE_OPTIONS=--experimental-vm-modules jest --setupFilesAfterEnv=./tests/setup.js --runInBand tests/unit",
    "test:integration": "cross-env NODE_ENV=test EMAIL_ENABLED=false NODE_OPTIONS=--experimental-vm-modules jest --setupFilesAfterEnv=./tests/setup.js --runInBand tests/integration",
    "email:test": "node scripts/testBrevoEmail.js",
    "email:overdue": "node jobs/sendOverdueReportReminders.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@getbrevo/brevo": "^6.0.3",
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.4",
    "morgan": "^1.12.0",
    "socket.io": "^4.8.3",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.5.1",
    "mongodb-memory-server": "^11.2.0",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
