export const REVIEW_ACTIONS = Object.freeze({
    CHANGES_REQUESTED: "changes_requested",
    APPROVED: "approved",
});

export const REVIEW_ACTION_VALUES = Object.freeze(
    Object.values(REVIEW_ACTIONS)
);